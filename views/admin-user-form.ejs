<div class="admin-form-wrapper">

    <div class="admin-page-header">

        <div>

            <h1>
                <%= admin ? "Edit Administrator" : "Create Administrator" %>
            </h1>

            <p>
                Create and manage administrator accounts securely.
            </p>

        </div>

        <a href="/admin/admin-users" class="back-btn">
            ← Back
        </a>

    </div>


    <% if(error){ %>

        <div class="admin-error">

            <%= error %>

        </div>

    <% } %>


    <form
        method="POST"
        action="<%= admin ? '/admin/admin-users/' + admin.id : '/admin/admin-users' %>">


<div class="section-title">

    <span class="section-icon">👤</span>

    <h2>Administrator Information</h2>

</div>

            <div class="admin-card-body">

                <div class="admin-grid">

                    <div class="field">

                        <label>Display Name</label>

                        <input
                            type="text"
                            name="display_name"
                            value="<%= admin ? admin.display_name : '' %>"
                            required>

                    </div>

                    <div class="field">

                        <label>Admin Title (Designation)</label>

                        <input
                            type="text"
                            name="title"
                            placeholder="e.g., Samiti Incharge, President"
                            value="<%= admin ? (admin.title || '') : '' %>">

                    </div>

                    <div class="field">

                        <label>Username</label>

                        <input
                            type="text"
                            name="username"
                            value="<%= admin ? admin.username : '' %>"
                            required>

                    </div>

                    <div class="field">

                        <label>Google Email</label>

                        <input
                            type="email"
                            name="google_email"
                            value="<%= admin ? admin.google_email : '' %>">

                    </div>

                    <div class="field">

                        <label>Role</label>

                        <select name="role">

                            <option value="admin"
                                <%= admin && admin.role==="admin" ? "selected":"" %>>
                                Administrator
                            </option>

                            <option value="super_admin"
                                <%= admin && admin.role==="super_admin" ? "selected":"" %>>
                                Super Administrator
                            </option>

                        </select>

                    </div>

                </div>

            </div>

        </div>



        <div class="admin-card">

            <div class="admin-card-header">

                <h2>🔒 Security</h2>

            </div>

            <div class="admin-card-body">

                <div class="field">

                    <label>Temporary Password</label>

                    <input
                        type="password"
                        name="password"
                        <%= admin ? "" : "required" %>>

                    <% if(admin){ %>

                        <small>
                            Leave blank to keep the existing password.
                        </small>

                    <% } %>

                </div>

            </div>

        </div>


        <div class="admin-actions">

            <a
                href="/admin/admin-users"
                class="secondary-btn">

                Cancel

            </a>

            <button
                type="submit"
                class="primary-btn">

                <%= admin ? "Update Administrator" : "Create Administrator" %>

            </button>

        </div>

    </form>

</div>